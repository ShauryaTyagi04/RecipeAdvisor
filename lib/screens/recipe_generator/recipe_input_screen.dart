import 'package:flutter/material.dart';

class RecipeInputScreen extends StatelessWidget {
  const RecipeInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
